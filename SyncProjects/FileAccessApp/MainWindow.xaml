<Window x:Class="FileAccessApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Access" Height="520" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="8">
            <WrapPanel Margin="0,0,0,8">
                <Button Name="BtnStartFileOps" Click="BtnStartFileOps_Click" Width="220" Margin="4">Start 5 background write tasks</Button>
                <Button Name="BtnClearFile" Click="BtnClearFile_Click" Width="220" Margin="4">Clear file (WriterLock)</Button>
                <Button Name="BtnOpenFile" Click="BtnOpenFile_Click" Width="140" Margin="4">Open file</Button>
            </WrapPanel>
            <TextBlock Text="File: file_ops.txt" Margin="4"/>
        </StackPanel>
        <GroupBox Header="Logs" Grid.Row="2" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <ListBox Name="LogList" Grid.Column="0" Margin="6" />
                <StackPanel Grid.Column="1" Margin="6" VerticalAlignment="Top">
                    <Button Name="BtnClearLogs" Click="BtnClearLogs_Click" Margin="0,0,0,8">Clear logs</Button>
                    <TextBlock Text="Status:" FontWeight="Bold"/>
                    <TextBlock Name="TxtStatus" Text="Ready" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
